@model IEnumerable<dynamic>

<div class="col-md-3">
    <h2>Categorieën</h2>
    <div class="list-group" id="categorieen">
        @foreach (var category in Model)
        {
            <a class="list-group-item list-group-item-action @(category.CategoryId == Model.First().CategoryId ? "active" : "")"
               id="@($"tab-{category.CategoryId}")" data-bs-toggle="pill" href="#@($"category-{category.CategoryId}")">
                @category.Name
            </a>
        }
    </div>
</div>